export default function Header() {
  return (
    <header
      style={{
        display: "flex",
        justifyContent: "center",
        padding: "24px 0",
      }}
    >
      <img src="/images/logo.png" alt="Infixo Logo" width="180" />
    </header>
  );
}
